COMMANDES POUR AFFICHER LES COURBES



Geiger (graphes):
python3 main.py --graphe geiger_1_distance
python3 main.py --graphe geiger_1_tension
python3 main.py --graphe geiger_2_distance
python3 main.py --graphe geiger_2_tension
python3 main.py --graphe attenuation_plomb
python3 main.py --graphe geiger_2_distance_avec_plomb

Geiger (courbes avec fit):
python3 main.py --fit geiger_1_distance_modele_1
python3 main.py --fit geiger_2_distance_modele_1
python3 main.py --fit geiger_2_distance_modele_2
python3 main.py --fit attenuation_plomb_modele_1
python3 main.py --fit attenuation_plomb_modele_2

Geiger (pipeline complet):
# 1) Donnees brutes dans: Mesures/Geiger/Données brutes
#    Fichiers attendus:
#    - geiger_1_grille.csv
#    - geiger_2_grille.csv
#    - geiger_1_plomb.csv
#    - geiger_2_plomb.csv
# 2) Regenerer les CSV par experience dans Mesures/Geiger
python3 traitement_geiger.py



Imageur beta (pipeline complet):
# 1) Donnees brutes dans: Mesures/Imageur_beta/Données brutes (format: tension-debit-numero.csv)
# 2) Agrege les courbes en fichiers tension-debit.csv a la racine de Mesures/Imageur_beta
# 3) Extrait les parametres de fit gaussien dans:
#    Mesures/Imageur_beta/extractions_gaussiennes.csv
python3 traitement_imageur_beta.py

Imageur beta (afficher I_max en fonction de la tension, debit=40):
python3 courbes_et_fits.py imageur-i-max-vs-tension --debit 40

Imageur beta (afficher largeur a mi-hauteur en fonction de la tension, debit=40):
python3 courbes_et_fits.py imageur-fwhm-vs-tension --debit 40

Imageur beta (afficher I_max en fonction du debit, tension=6000):
python3 courbes_et_fits.py imageur-i-max-vs-debit --tension 6000

Imageur beta (afficher largeur a mi-hauteur en fonction du debit, tension=6000):
python3 courbes_et_fits.py imageur-fwhm-vs-debit --tension 6000
